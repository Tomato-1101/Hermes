{
  "name": "@hermes/engine",
  "version": "0.0.1",
  "private": true,
  "description": "Hermes Flow IR execution engine: dispatcher, run context, retry/timeout policies, structured control flow.",
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "test": "vitest",
    "test:run": "vitest run",
    "clean": "rm -rf dist .turbo *.tsbuildinfo"
  },
  "dependencies": {
    "@hermes/ir": "workspace:*",
    "mitt": "^3.0.1"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
